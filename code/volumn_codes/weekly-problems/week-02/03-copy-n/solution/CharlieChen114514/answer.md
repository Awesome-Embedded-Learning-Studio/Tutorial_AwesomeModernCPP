随手写的hh, 图一乐。我自己使用span处理的这个

span 把"这段缓冲区有多长"的信息随身带着,`dst.size()` 就是那个 n,"正好写满 n 格"的要求直接体现在参数类型上,数组传进来自动推长度。

咱们看函数体,就两步:`strlen` 量出 src 实际多长,和 `dst.size()` 取小,得到"真字符"的个数;`memcpy` 抄真字符,`memset` 把剩下的格子统一填 `'\0'`。原版循环里那个"撞到 `'\0'` 就切去补零"的分支,在这里压根不存在。
