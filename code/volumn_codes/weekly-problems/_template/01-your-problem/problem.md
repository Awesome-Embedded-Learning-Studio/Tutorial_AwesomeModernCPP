<!-- 题面:两三句话讲清任务,不写 frontmatter,这里的内容会在题卡里渲染成 markdown。
     写清楚:输入/参数是什么、要求返回什么、必须满足的约束(如「必须用递归」)。
     判题类最好给一两个具体例子,学员没有判题数据时全靠例子理解题意。 -->

给定……,编写函数 `your_function(n)`,返回……。**必须……**。

示例:

- `your_function(0)` 应返回 `1`
- `your_function(10)` 应返回 `55`
