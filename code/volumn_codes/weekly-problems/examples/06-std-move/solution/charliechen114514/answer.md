`std::move(x)` **什么都不移动**。它只是 `static_cast<T&&>(x)` 的漂亮包装——把表达式标成「右值」,好让重载决议选中移动构造/移动赋值;它本身编译后通常**零指令**,一个字节都没搬。

真正搬数据的是随后被选中的**移动构造函数**:它「偷」走源对象的资源句柄(比如 `std::vector` 的堆指针),把源对象留在有效但未指定的状态。快就快在:**浅浅地转个指针,而不是深深地拷一整片堆内存**。

一句话:`std::move` 是「表态」,移动构造才是「动手」。
