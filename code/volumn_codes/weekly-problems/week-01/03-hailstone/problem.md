冰雹序列从正整数 `n` 出发:偶数就除以 2,奇数就乘 3 加 1,直到变成 1。例如 `n = 6` 的序列是 `6 3 10 5 16 8 4 2 1`。

写一个**生成器函数** `hailstone(n)`,产出从 `n` 开始的冰雹序列;**到达末尾后无限期地产出 1**——「无穷」不吓人,这正是生成器的舒适区。`main` 里消费它:从标准输入读 `n`,打印序列直到(含)第一个 `1`,数字用空格分隔。

要求使用 C++23 的 `std::generator`(`<generator>` 头)。判题环境是 gcc 15 / `-std=c++23`,放心用。
