对于一个正整数,**digit distance** 指相邻两位数字之差的绝对值之和。例如:

- `6` 的 digit distance 是 `0`(只有一位,没有相邻对);
- `61` 的是 `5`(`|6 - 1|`);
- `71253` 的是 `12`(`|7-1| + |1-2| + |2-5| + |5-3| = 6 + 1 + 3 + 2`)。

编写函数 `digit_distance(n)`,返回 `n` 的 digit distance。**必须使用递归**。
